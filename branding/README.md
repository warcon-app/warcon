# Warcon brand

The mark is a brass plate with two cut corners and a reticle; the wordmark is **WARCON** set in
Barlow Condensed SemiBold with 6% tracking, traced to outlines so nothing depends on installed fonts.

Everything in this folder is generated by `bun run scripts/branding.ts`. Edit the script, not the
files.

## Files

| File                                             | Use                                                                                                           |
| ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- |
| `warcon-logo-on-dark.svg` / `-on-light.svg`      | Primary horizontal lockup. Pick by background.                                                                |
| `warcon-logo-mono-white.svg` / `-mono-black.svg` | Single-colour lockup for print, embroidery, forced-colour modes.                                              |
| `warcon-stacked-on-dark.svg` / `-on-light.svg`   | Square-ish lockup for avatars and tiles.                                                                      |
| `warcon-mark.svg`, `warcon-mark-mono-*.svg`      | Mark alone (favicon, app icon, small spaces under 24px).                                                      |
| `warcon-wordmark-on-dark.svg` / `-on-light.svg`  | Wordmark alone.                                                                                               |
| `png/`                                           | Rasterised copies: mark at 16–1024px, lockups at 2x, stacked at 1024px wide, and the 1280×640 social preview. |

## Colours

| Token | Hex       | Role                                |
| ----- | --------- | ----------------------------------- |
| Brass | `#D4A843` | Accent, the mark's plate            |
| Ink   | `#0F0F12` | Dark background, glyph on the plate |
| Mist  | `#E8E8E8` | Text on dark                        |

Keep the plate brass whenever colour is available; the mono variants exist for one-colour contexts
only. Do not recolour, outline, rotate, add effects, or set the wordmark in another face.

## Clear space and minimum size

Leave clear space around the lockup equal to the height of the reticle's centre dot times 4 (about
a quarter of the mark's height). Use the mark alone below 24px in height; the horizontal lockup
stays legible down to 20px high.

## Fonts and licence

Barlow Condensed is licensed under the SIL Open Font License (`src/OFL.txt`). The outlines in these
SVGs are derived works you may use freely with the logo. The logo files themselves are covered by
the repository's MIT licence; please do not use them to imply that a hosted service is run by the
Warcon project.
